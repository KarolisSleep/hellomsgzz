/* var welcomeMsg = document.getElementById("welcome-msg");

welcomeMsg.onclick = function () {
    welcomeMsg.addEventListener("welcome-msg"), () => {
        welcomeMsg.className.add("on-screen");
    }
}; */

